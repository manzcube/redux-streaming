import { createBrowserHistory } from 'history'; // This history is from a library that react-router-dom relies on(depends up on) as dependency.
export default createBrowserHistory(); // This is basically for redirecting in react